---
dependency:
  name: galaxy
driver:
  name: docker
# lint:
#   name: ansible-lint
platforms:
  - name: nginx-server
    image: ubuntu:trusty
    command: sleep infinity
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: default
verifier:
  name: testinfra
  lint:
    name: flake8
